variables:                        # Good to have a comment here, for example:
  NEXUS_REGISTRY: nexus.prjx.uk/repository/
  NEXUS_REGISTRY_USER: admin
  NEXUS_REGISTRY_PASSWORD: kz77-9prsLxHNojFffv


# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Docker.gitlab-ci.yml

# Build a Docker image with CI/CD and push to the GitLab registry.
# Docker-in-Docker documentation: https://docs.gitlab.com/ee/ci/docker/using_docker_build.html
#
# This template uses one generic job with conditional builds
# for the default branch and all other (MR) branches.

stages:
  - ""


docker-build:
  stage: build
  # Use the official docker image.
  image: docker:latest

  services:
    - docker:dind
  before_script:
    - docker login -u "admin" -p kz77-9prsLxHNojFffv nexus.prjx.uk/repository/
  # Default branch leaves tag empty (= latest tag)
  # All other branches are tagged with the escaped branch name (commit ref slug)
  script:
    - |
      if [[ "$CI_COMMIT_BRANCH" == "$CI_DEFAULT_BRANCH" ]]; then
        tag=""
        echo "Running on default branch '$CI_DEFAULT_BRANCH': tag = 'latest'"
      else
        tag=":$CI_COMMIT_REF_SLUG"
        echo "Running on branch '$CI_COMMIT_BRANCH': tag = $tag"
      fi
      echo "Registry is $NEXUS_REGISTRY"
      echo "Project is $CI_PROJECT_NAME"
      echo "Project is $tag"
      echo "Full call is $NEXUS_REGISTRY/$CI_PROJECT_NAME${tag}"
    - docker build --pull -t "$NEXUS_REGISTRY$CI_PROJECT_NAME${tag}" .
    - docker push "$NEXUS_REGISTRY$CI_PROJECT_NAME${tag}"
  # Run this job in a branch where a Dockerfile exists
  rules:
    - if: $CI_COMMIT_BRANCH
      exists:
        - Dockerfile

notify:
  stage: notify
  when: on_failure
  image: alpine:latest
  script:
    - 'apk --no-cache add curl'
    - 'curl -X POST -d "body=test message" http://syn-01.prjx.uk:8009/notify/alert-net'

